package com.parseable.temporal.version;

/** Plugin version — kept in sync with pom.xml. */
public final class Version {
  public static final String PLUGIN_VERSION = "0.1.0";

  private Version() { }
}
